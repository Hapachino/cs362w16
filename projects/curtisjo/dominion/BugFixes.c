BugFixes.c
playSmithy:
I fixed the bug in the draw card for loop so that i is initialized to 0. 

playAdventurer:
Fixed the bug in the discard while loop so that the condition is z-1>=0.

playCouncil_room
Fixed the bug in the drawcard for loop so that i is initialized to 0.

playRemodel
Fixed bug so that the card will be trashed
j = state->hand[currentPlayer][choice1] 

