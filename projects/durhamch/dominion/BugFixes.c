/*
 * Fixes were made to:
 * smithy_function: for loop runs 3 times instead of 4 now
 * adventurer_function: while loop is now > instead of >=
 * council: implemented fix according to Lorena's guidance
 *
 * Other teammate never communicated. Never received guidance
 * on any other bugs that should be fixed
 */
