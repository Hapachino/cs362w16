Changes: smithy for loop increased by one interation
Adventurer: changed if statement to remove copper
Council room:
Changed the if statement in for loop from if I not currentPlayer to if I is current player.
Village:
Add this currentPlayer = currentPlayer + 1;
Before discard so the card comes out of the wrong players hand
Steward: I change I switched the integers in the if and else if statement so that  the wrong action is happening
