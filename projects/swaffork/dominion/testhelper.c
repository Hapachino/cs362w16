/* Unit Test Helper Extraordinaire for Dominion */
#include "testhelper.h"
#include "dominion.h"
#include "dominion_helpers.h"
#include "rngs.h"


int* stateDiff(struct gameState* state1, struct gameState* state2)
{
    // make array to hold flags
    // iterate through state arrays
    // if state1[i] == state2[i]
    //  stateDiff[i] = 0
    // else
    //  stateDiff[i] = 1
}
